package com.saker.dorak.Repository;

import com.saker.dorak.Entity.admin;
import org.springframework.data.jpa.repository.JpaRepository;

public interface adminRepository extends JpaRepository<admin, Long> {
}
