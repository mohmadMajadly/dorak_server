package com.saker.dorak.Repository;

import com.saker.dorak.Entity.worker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface workerRepository extends JpaRepository<worker, Long> {
}
