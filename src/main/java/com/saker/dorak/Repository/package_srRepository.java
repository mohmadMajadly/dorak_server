package com.saker.dorak.Repository;

import com.saker.dorak.Entity.package_sr;
import org.springframework.data.jpa.repository.JpaRepository;

public interface package_srRepository extends JpaRepository<package_sr, Long> {
}
