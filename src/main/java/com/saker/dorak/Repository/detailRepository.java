package com.saker.dorak.Repository;

import com.saker.dorak.Entity.detail;
import org.springframework.data.jpa.repository.JpaRepository;

public interface detailRepository extends JpaRepository<detail, Long> {
}
